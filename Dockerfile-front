FROM envoyproxy/envoy:latest

COPY envoy-front.json /etc/envoy.json
COPY ls_access_token /etc/ls_access_token
EXPOSE 80
EXPOSE 8001
CMD /usr/local/bin/envoy -c /etc/envoy.json --service-cluster front-proxy